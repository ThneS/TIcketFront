{
  "dataSources": {
    "showsList": "backend",
    "showDetail": "contract"
  },
  "mergePolicy": {
    "defaultMode": "coalesce",
    "listFields": {
      "description": "preferBackend",
      "location": "preferBackend"
    },
    "detailFields": {
      "description": "preferContract",
      "location": "preferBackend"
    }
  }
}
